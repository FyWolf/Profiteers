<%- include('../partials/map-editor', {
    title:       title,
    apiBase:     '/operations/' + operation.id + '/map',
    backUrl:     '/operations/' + operation.id,
    headerTitle: operation.title + ' — MISSION MAP',
    canEdit:     canEdit,
    squads:      squads,
    planContext: null,
    user:        user
}) %>
</body>
</html>
